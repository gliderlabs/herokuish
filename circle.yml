machine:
  services:
    - docker

dependencies:
  override:
    - make build

test:
  pre:
    - docker pull progrium/cedarish
  override:
    - tests/shunit2:
        parallel: true
        files:
          - tests/**/tests.sh
