machine:
  services:
    - docker

dependencies:
  pre:
    - go get github.com/jteeuwen/go-bindata/...
  override:
    - make build

test:
  pre:
    - docker pull progrium/cedarish
  override:
    - tests/shunit2:
        parallel: true
        files:
          - tests/**/tests.sh
